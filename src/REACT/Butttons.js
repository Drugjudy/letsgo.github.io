import { Button } from "react-bootstrap";

const Buttons = () => {
    return(
        <div >
      <a href="#cards">
  <Button className="button" variant="light">Lets Start</Button>{' '}
 </a>

</div>
    );
};

export default Buttons;
